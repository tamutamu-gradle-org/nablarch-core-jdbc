sudo: required

language: java

jdk:
  - openjdk6

os:
  - linux

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
    
before_install:
 - echo $JAVA_HOME
 - ls -l $JAVA_HOME/jre/lib/security
 - chmod +x gradlew
 - ./gradlew clean

script:
  - ./gradlew build
